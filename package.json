{
  "name": "add-atcodex-comment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "check": "node --check post-comment.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json', 'utf8'))\"",
    "test:e2e": "node --test tests/extension.test.js"
  },
  "devDependencies": {
    "eslint": "10.8.1",
    "puppeteer": "25.6.0"
  }
}
